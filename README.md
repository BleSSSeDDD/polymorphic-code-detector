# x86 Code Emulator & Polymorphic Code Detector

**Анализ и эмуляция x86-кода с детектированием полиморфного поведения**

## О проекте

Эмулятор процессора x86, написанный на C, который:
- **Эмулирует основные инструкции x86** (MOV, ADD, SUB, XOR, PUSH, POP, INC, DEC, XCHG, STOSW)
- **Детектирует полиморфный код** через анализ сигнатур и поведенческих паттернов
- **Работает с COM-файлами** формата DOS

## Архитектура

### Основные компоненты
- **Эмулятор регистров** (AX, BX, CX, DX, SI, DI, BP, SP, сегментные регистры)
- **Стек процессора** (256 слов)
- **Декодер инструкций** с поддержкой различных режимов адресации
- **Система эмуляции** исполняемого кода

### Поддерживаемые инструкции
```c
MOV_R_R()      // Move register to register
MOV_R_IMM16()  // Move immediate to register  
MOV_R_SR()     // Move segment register to register
ADD_AX()       // Add immediate to AX
SUB_R_R()      // Subtract register from register
XOR_R_IMM16()  // XOR register with immediate
PUSH()/POP()   // Stack operations
// ... и многие другие
```

### Пример работы
```
----------------------- POLYMORPHIC CODE DETECTOR -----------------------

Enter the path: C:/samples/suspicious.com
Analyzing file...
[EMULATION] MOV AX, 0x1000
[EMULATION] XOR AX, 0x00FF
[EMULATION] PUSH AX
[RESULT] Polymorphic signature detected!
```

## 🔍 Технические детали

### Особенности реализации
- **Обработка little-endian** данных через `reverse()`
- **Эмуляция сегментной архитектуры** x86
- **Полная система стековых операций** с проверкой переполнения
- **Поддержка IMM8/IMM16** операций

### Алгоритм детектирования
1. **Загрузка и парсинг** COM-файла
2. **Последовательная эмуляция** инструкций
3. **Анализ поведенческих паттернов**
4. **Сравнение с сигнатурами** полиморфного кода


## Требования
- Компилятор C (GCC, Clang)
- Совместимость с x86/x64 системами
- Доступ к файловой системе для анализа файлов

---

**Код эмулирует реальное выполнение инструкций процессора, что делает его ценным для анализа безопасности и реверс-инжиниринга.**

---
